Options -MultiViews

RewriteEngine On
RewriteBase /

# Sert les fichiers et dossiers existants normalement.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React Router prend en charge toutes les autres adresses.
RewriteRule ^ index.html [L]
