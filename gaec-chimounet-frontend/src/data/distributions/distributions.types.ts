export type DistributionDate = {
    id: string;
    date: string;
    location: string;
    isPublished: boolean;
};

export type DistributionDateInput = {
    date: string;
    location: string;
    isPublished: boolean;
};
